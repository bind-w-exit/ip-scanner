<Page
    x:Class="IpScanner.Ui.Pages.ScanPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:IpScanner.Ui.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    mc:Ignorable="d"
    xmlns:convertors="using:IpScanner.Ui.Convertors"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Resources>
        <convertors:BoolToVisibilityConvertor x:Key="BoolToVisibilityConvertor" />
        <convertors:ValidationErrorToColorConverter x:Key="ValidationErrorToColorConverter" />
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>

        <!-- Action bar -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Padding="5,0,5,0" Margin="0">
            <Button x:Name="ScanButton" Command="{Binding ScanCommand}" Margin="0 0 5 0">
                <StackPanel Orientation="Horizontal">
                    <FontIcon Glyph="&#xE768;" Foreground="Green"/>
                    <TextBlock x:Uid="Scan" Margin="5,0,0,0" ></TextBlock>
                </StackPanel>
            </Button>
            <Button x:Name="StopButton" Command="{Binding CancelCommand}" Margin="5,0,5,0">
                <StackPanel Orientation="Horizontal">
                    <FontIcon Glyph="&#xE769;"/>
                </StackPanel>
            </Button>

            <StackPanel Orientation="Vertical">
                <AppBarSeparator Style="{StaticResource GraySeparatorStyle}" />
            </StackPanel>

            <Button Margin="5,0,5,0" Command="{Binding SetSubnetMask}">
                <StackPanel Orientation="Horizontal">
                    <FontIcon Glyph="&#xF6FA;" Foreground="DodgerBlue"/>
                </StackPanel>
                <ToolTipService.ToolTip>
                    <ToolTip x:Uid="Subnet"/>
                </ToolTipService.ToolTip>
            </Button>

            <Button Margin="5,0,5,0" Command="{Binding SetSubnetClassCMask}">
                <StackPanel Orientation="Horizontal">
                    <FontIcon Glyph="&#xED5C;" Foreground="DodgerBlue"/>
                </StackPanel>
                <ToolTipService.ToolTip>
                    <ToolTip x:Uid="SubnetC"/>
                </ToolTipService.ToolTip>
            </Button>

            <StackPanel Orientation="Vertical">
                <AppBarSeparator Style="{StaticResource GraySeparatorStyle}" />
            </StackPanel>

            <Button Margin="5,0,5,0">
                <StackPanel Orientation="Horizontal">
                    <FontIcon Glyph="&#xEC8F;" />
                </StackPanel>
                <ToolTipService.ToolTip>
                    <ToolTip x:Uid="Expand"/>
                </ToolTipService.ToolTip>
            </Button>

            <Button Margin="5,0,5,0">
                <StackPanel Orientation="Horizontal">
                    <FontIcon Glyph="&#xF165;" />
                </StackPanel>
                <ToolTipService.ToolTip>
                    <ToolTip x:Uid="Collapse"/>
                </ToolTipService.ToolTip>
            </Button>
        </StackPanel>

        <!-- Input bar -->
        <Grid Grid.Row="1" Padding="5">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*"></ColumnDefinition>
                <ColumnDefinition Width="1*"></ColumnDefinition>
            </Grid.ColumnDefinitions>

            <TextBox x:Uid="IpRangeInput" Grid.Column="0" Text="{Binding IpRange, Mode=TwoWay}" Margin="0,0,2,0"></TextBox>
            <TextBox x:Uid="Search" Grid.Column="1" Text="{Binding SearchText, Mode=TwoWay}" 
                     Margin="2,0,5,0"></TextBox>

            <TextBlock x:Uid="IpRangeValidationMessage" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Foreground="{StaticResource ErrorColorBrush}"
                    Visibility="{Binding HasValidationError, Converter={StaticResource BoolToVisibilityConvertor}}"
                    Margin="2,0,5,0"/>
        </Grid>

        <!-- Devices list -->
        <Grid Grid.Row="2" Padding="5">
            <controls:DataGrid x:Name="ScannedDevicesDataGrid" ItemsSource="{Binding ScannedDevices, Mode=TwoWay}" 
                           AutoGenerateColumns="False" CanBeScrollAnchor="True" ScrollViewer.IsVerticalRailEnabled="True"
                           ScrollViewer.VerticalScrollBarVisibility="Visible" ScrollViewer.VerticalScrollMode ="Enabled">
                <controls:DataGrid.Columns>
                    <controls:DataGridTextColumn Header="Status" Binding="{Binding Status}" />
                    <controls:DataGridTextColumn Header="Name" Binding="{Binding Name}" />
                    <controls:DataGridTextColumn Header="IP Address" Binding="{Binding Ip}"/>
                    <controls:DataGridTextColumn Header="Manufacturer" Binding="{Binding Manufactor}" />
                    <controls:DataGridTextColumn Header="MAC Address" Binding="{Binding MacAddress}" />
                    <controls:DataGridTextColumn Header="Comments" Binding="{Binding Comments}" Width="*" />
                </controls:DataGrid.Columns>
            </controls:DataGrid>
        </Grid>

        <!-- Progress bar -->
        <ProgressBar Grid.Row="3" Value="{Binding Progress, Mode=TwoWay}" Maximum="{Binding CountOfScannedIps, Mode=TwoWay}" 
                     IsIndeterminate="True"></ProgressBar>
    </Grid>
</Page>
